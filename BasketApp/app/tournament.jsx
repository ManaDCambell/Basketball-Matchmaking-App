import React from 'react';
import { View, Text } from 'react-native';

const Tournament = () => {
    const [tournamentData] = useState(null);
    return (
        <View>
            <Text>Tournament Generation</Text>
        </View>
    )
}

export default Tournament;